# Исключаем только исходники TypeScript
src/
*.ts
*.tsx

# Исключаем файлы разработки
tsconfig.json
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Исключаем тесты
test/
tests/
*.test.ts
*.test.js

# Исключаем временные файлы
.vscode/
.vscode-test/
*.vsix

# Исключаем IDE файлы
.idea/
*.swp
*.swo

# Исключаем OS файлы
.DS_Store
Thumbs.db

# Исключаем логи
*.log

# Исключаем TypeScript build info
*.tsbuildinfo

# Включаем все остальное (включая node_modules)
!node_modules/
!out/